generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Receita{
  id Int @id @default(autoincrement ())
  nome String
  ingredientes String
  modoPreparo String
  imagem String?
}
